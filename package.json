{
  "name": "pr-coverage-annotations",
  "version": "1.0.0",
  "type": "module",
  "description": "GitHub Action that annotates PRs with uncovered code from LCOV coverage reports",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && cp dist/index.js dist/entry.cjs && ncc build dist/entry.cjs -o dist --license licenses.txt",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "@types/node": "^20.10.0",
    "@types/node-fetch": "^2.6.11",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@octokit/rest": "^20.0.1",
    "actions-toolkit": "^6.0.1",
    "lcov-result-merger": "^4.1.0",
    "node-fetch": "^2.6.12"
  }
}