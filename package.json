{
  "name": "pr-coverage-annotations",
  "version": "1.0.0",
  "type": "module",
  "description": "GitHub Action that annotates PRs with uncovered code from LCOV coverage reports",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.2.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "esbuild": "^0.24.0"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@octokit/rest": "^22.0.1",
    "actions-toolkit": "^6.0.1",
    "lcov-result-merger": "^5.0.1",
    "node-fetch": "^3.3.2"
  }
}